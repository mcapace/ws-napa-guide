# Reference examples

Screen recordings, captures, and other reference material for matching **therealhotels.com** (scroll hero, detail pages, rhythm).

| File | Description |
|------|-------------|
| `therealhotels-scroll-reference-2026-04-03.mov` | Screen recording (Apr 3, 2026) for scroll / UX reference |

Original filename on disk: `Screen Recording 2026-04-03 at 10.03.57 AM.mov`.

**Git:** Large `.mov` files are tracked with [Git LFS](https://git-lfs.github.com/). Clone with Git LFS installed (`git lfs install`) so these pointers resolve to real video.
